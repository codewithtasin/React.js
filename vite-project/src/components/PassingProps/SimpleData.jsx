// Passing Properry using simple Data

const SimpleData = (props) => {
  return <div>
    <h1> {props.title}</h1>
    <p> {props.des} How are you? I am fine. </p>
  </div>;
};

export default SimpleData;
